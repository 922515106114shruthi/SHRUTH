#include<stdio.h>
#include<conio.h>
void main()
{
int a[20],n,i;
printf(" enter the range:");
scanf("%d",&n);
printf(" enter the first range:");
scanf("%d",&f);
for(i=f;i<=n;i++)
{
scanf("%d",&a[i]);
}
for(i=f;i<=n;i++)
{
add=((a[f]+a[n])*n)/2;
}
printf("%d",add);
getch();
}
